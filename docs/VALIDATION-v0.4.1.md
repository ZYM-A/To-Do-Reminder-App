# v0.4.1 验证记录

日期：2026-09-14

## 结果

- `:app:assembleDebug`：通过，versionName 0.4.1 / versionCode 5。
- `:app:testDebugUnitTest`：61 项通过，0 失败、0 错误、0 跳过。
- `:app:lintDebug`：No issues found。
- 新增导航回归：从三个主栏目分别打开伴侣页面，返回原栏目；底部仍有三个入口，待办筛选与提醒设置保留。
- 原有待办、日记、数据库升级和农历纪念日测试通过。
- APK 签名与原安装包一致，可覆盖安装。
- `git diff --check`：通过。

APK SHA-256：`63d4b7ebaaeccf2b3077ea616c6009c7cb7f45f34f5ba6b1337bd15d353a17f0`

## 功能范围

本次仅实现右上角 AI 伴侣入口与独立页面。页面明确显示聊天尚未开放，不提供模型接入、人设编辑、聊天发送或记忆功能。

伴侣页面仅接收返回回调，没有日记、日程或数据库依赖；应用未增加联网权限。数据库结构不变。

## 上传检查

审查本次源文件与提交差异，对当前仓库文件和 APK 扫描常见密钥特征及敏感文件类型，未发现命中；APK assets 仅包含开源许可证。没有新增个人照片、用户数据库或私人配置。

忽略规则补充本地数据库、私有目录和证书文件。上传限定源码、文档、虚构测试及构建产物，不包含本机工具、配置或用户内容。此检查不代表对历史提交完成全面隐私审计。

## 范围限制

自动操作测试运行于 Robolectric Android 9（API 28），尚未进行实体手机验收。
